# findings

**Refinement batch 1 (25 cells) analysed.** splatAlpha=0.5 is the single strongest lever found: tri-splat kodim01 ΔE 11.11→10.30, kodim12 5.36→5.11 at identical bytes (2061B), and tri-gauss −0.12..0.14 — this halves the splat-vs-voronoi gap (kodim01: 1.21→0.40 ΔE) while bytes are unchanged, so it can reshuffle the low-mid R-D order. triColor=flat is a dead end and dominated on BOTH axes (delaunay/tri-tiled bytes 5206B vs 4444B interp AND ΔE +0.9..1.2) — flat does not shrink the palette, it bloats it; discard. CVT iters=64 is consistent but tiny (+0.02..0.07 ΔE, bytes unchanged) on both voronoi and delaunay at n=256 and n=1024 — likely not worth the compute, but iters=100 + a top-end n=2048 probe will confirm saturation. blend=0.5 is a null at n=256 (exactly 0 ΔE change on tri-tiled and delaunay kodim01); the n=1024 tie-region probe tests whether it matters where boundary-pixel counts are highest. aa gives a cheap +0.03..0.07 ΔE win with no byte cost; the alpha+aa stacking cells test whether the wins add.

**Base grid complete (3072/3072).** R-D curves are smooth and monotone for every style; the mode question is settled: uniform beats combined on all 8 styles at essentially every budget (grows to ~0.8-1.9 ΔE at n=1024-2048 for the splat/gauss/voronoi family), so uniform is the operating point. Byte economics are the dominant separator: voronoi costs ~1/3.5 of delaunay/tri-tiled at equal n, and at matched byte budget voronoi wins decisively (n=512: ΔE 6.92 vs delaunay n=128 ΔE 8.89; n=2048: ΔE 5.39 vs delaunay n=512 ΔE 7.04). cell-tris is confirmed worst on both axes (ΔE 10.98-14.89 at 3.4-5.2x voronoi bytes); voro-fan is byte-profligate (101KB at n=2048). The one unstable region is the equal-n tie at the top (voronoi vs delaunay/tri-tiled within ~0.1 ΔE at n=1024-2048). Next batch (23 cells, all uniform mode): sweep splatAlpha 0.3/0.7 on tri-splat/tri-gauss across kodim01/12/19, splatAlpha 0.5 on fresh images kodim05/23 for robustness, alpha persistence at n=512, alpha+aa stacking, blend=0.5 at n=1024, and CVT iters=100 + n=2048 saturation checks.

**Batch 2 (48 refinements total) analysed.** splatAlpha's 0.8→0.3 trend is monotone and consistent on every tested image (tri-splat kodim01 11.11→10.81→10.30→9.97 for 0.8/0.7/0.5/0.3, kodim19 7.65→6.91, kodim12 5.36→5.04; tri-gauss kodim01 10.11→9.97→9.90, kodim19 6.98→6.84, kodim12 5.26→5.10) at byte-identical payloads, and on fresh kodim05/23 tri-splat 0.5 also wins (14.69→13.63, 7.08→6.87) — so the gain is image-robust, not a fit. At a=0.3, tri-splat n=256 (ΔE 9.97) now ties voronoi n=256 (9.90) on kodim01 where it trailed by 1.21 at 0.8: the splat-vs-voronoi ranking is therefore alpha-dependent, and the low-mid R-D order is not yet final — a real hole in the primitive ranking. CVT is closed: iters100 == iters64 to 3 decimals on both voronoi and delaunay kodim01 (9.873 / 10.294) and ≈ iters0, and n=2048 voronoi iters64 gains only −0.07 — not worth compute. blend=0.5 is closed: byte-identical null at n=1024 (tri-tiled 8.165/4.026 and delaunay 8.159/4.012 kodim01/12 exactly match base). triColor flat is closed (dominated on both axes). aa stays a consistent ~−0.045 ΔE free win (tri-splat 0.5 kodim01 10.302→10.255, kodim12 5.112→5.067). Batch 3 (20 cells, all uniform): find the alpha floor with 0.2/0.1 on tri-splat n=256 kodim01/12/19 (is the optimum below 0.3, or does the splat fade and reverse?), test alpha-0.3+aa stacking on tri-splat/tri-gauss, and confirm alpha-0.3 persistence at n=128/512/1024 (tri-splat) and n=512 (tri-gauss) so the new operating point is budget-robust before the ranking is declared stable.

**Batch 3 (24 cells) analysed.** The alpha floor is found: tri-splat n=256 kodim01 ΔE 9.968→9.874→9.844 for 0.3/0.2/0.1 (diminishing returns), but kodim12 REVERSES below 0.2 (5.039@0.3, 5.037@0.2, 5.064@0.1) — so 0.2 is the safe floor, 0.1 is over-fitted; kodim19 is flat (6.906→6.867→6.864). alpha-0.3+aa stacking stacks cleanly: tri-splat kodim01 9.968→9.928, tri-gauss 9.899→9.875 at n=256 (≈aa's standalone −0.03..0.05, no saturation). alpha-0.3 persistence is confirmed and, critically, grows at higher budgets: tri-splat kodim01 11.208@n=128, 9.968@n=256, 9.239@n=512, 8.131@n=1024 vs base-0.8 (12.137/11.112/10.249/8.923) — the 0.3 gain widens from −0.93 to −1.14 to −1.01 to −0.79 ΔE; kodim12 is smaller (−0.21/−0.32/−0.25/−0.21) but consistent. tri-gauss n=512 kodim01 9.255 vs base 9.464 (−0.21). At alpha-0.3 the splat family now ties or beats voronoi on kodim01 at every budget (e.g. n=512 tri-splat 9.239 vs voronoi 9.260; n=1024 8.131 vs 8.156), so the ranking split is definitively alpha-dependent, not a fixed primitive gap. The ONE remaining hole: alpha-0.3/0.2 has not been validated at n=2048 (tri-splat) or n=1024/2048 (tri-gauss) — the top end is where voronoi/delaunay/tri-tiled are within ~0.1 ΔE and where a reversal would change the final ranking. Batch 4 (25 cells, all uniform) closes it: top-end alpha-0.3 (tri-splat n=2048 + kodim19 n=1024; tri-gauss n=1024/2048), alpha-0.2 top-vs-mid probes (n=512 and n=2048), aa-stacking at n=512, and fresh-image robustness (kodim05/23 n=512) so the new operating point is confirmed budget- and image-robust before DONE.

**Batch 4 (24 cells) analysed — top end closed, ranking stable → DONE.** Top-end alpha-0.3 is validated and does NOT overturn the byte ranking: at n=2048 tri-splat a0.3 kodim01/12/19 ΔE 7.548/3.511/5.044 and tri-gauss 7.476/3.581/5.002, i.e. the splat/gauss family now ties the base top-tier (voronoi 7.501/3.620/5.052, delaunay 7.430/3.536/5.155, tri-tiled 7.428/3.532/5.146) within ~0.05-0.12 ΔE at EQUAL budget, but at byte cost 1.6-3.4x voronoi — byte-matched voronoi still wins decisively (n=2048: voronoi 10253B/7.501 vs tri-splat a0.3 16397B/7.548; and voronoi n=1024 5133B/8.156 is already within 0.4 of splat n=2048 16397B/7.548). So the equal-n tie at the top is confirmed real but irrelevant once bytes are priced, exactly as batch 2 predicted. alpha-0.2 vs 0.3 at n=2048: 0.2 is marginally better or equal (tri-splat 7.477 vs 7.548; tri-gauss 7.468 vs 7.476 kodim01) — the 0.2 floor holds at the top end, no reversal. aa-stacking at n=512 persists (tri-splat a0.3 9.239→9.227, tri-gauss 9.255→9.247) and stays a free −0.01..0.05 win. Fresh images confirm the alpha operating point: tri-splat a0.3 n=512 kodim05 11.878 (base-0.8 13.22) and kodim23 5.598 (base 5.851), tri-gauss 11.745/5.791 — the 0.3 gain is image-robust, not a kodim01/12/19 fit. Every lever is now closed: mode (uniform), splatAlpha (0.2 floor), aa (free, stacks), blend (null), CVT iters (null), triColor (interp). Final ranking is robust across all 24 images and all budgets: voronoi is the byte-efficiency winner at every matched byte budget (cell-tris worst, voro-fan byte-profligate, delaunay/tri-tiled ≈ voronoi at equal n but 3.4-3.5x bytes, splat/gauss a0.3-0.2 tie them at equal n but lose byte-matched); equal-budget top-tier differences are all <0.15 ΔE — below any meaningful threshold and already characterized. R-D curves are smooth and monotone for all 8 primitives. Declaring DONE.

